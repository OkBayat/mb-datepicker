import { DateTriggerForDirective } from './date-trigger-for.directive';

describe('MenuTriggerForDirective', () => {
  it('should create an instance', () => {
    /*const directive = new DateTriggerForDirective();
    expect(directive).toBeTruthy();*/
  });
});
