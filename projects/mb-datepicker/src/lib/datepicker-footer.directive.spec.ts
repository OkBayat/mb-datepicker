import { DatepickerFooterDirective } from './datepicker-footer.directive';

describe('DatepickerFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new DatepickerFooterDirective();
    expect(directive).toBeTruthy();
  });
});
